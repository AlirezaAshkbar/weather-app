import React from 'react'

type Props = {}

const WeekForcast = (props: Props) => {
  return (
    <section className='flex w-full flex-col gap-4'>
        <p className='text-2xl'>Forcast (7 days)</p>
    </section>
  )
}

export default WeekForcast